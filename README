
AVR ISP
o quickdev standart cable connecto
o connects to a serial & isp combo cable
o connect avr dragon isp to isp header at combo cable
o avr dragon isp
    o vcc has to be connected
    o red: vcc
    o black: gnd
    o sck: yellow
    o mosi: green
    o rst: black
o runs under os x

AVR JTAG:
o connects to the xilinx jtag cable
o AVR ISP has to be disconnected
o jtag header
    o vcc: red
    o gnd: black
    o tdi: yellow
    o tdo: green
    o tms: white
    o sck: red


CPLD JTAG:
o preload usbdriver
o export LD_PRELOAD=/home/david/Devel/arch/fpga/tools/usb-driver
o check usb vendor id, has to be 03fd:0008
o otherwise fxload the firmware by hand
o start impact, it should find the cable automatically, if not reboot
o runs under linx in vmware

POC MASSSTORAGE
o makefile include 'programm' for avrdude with dragon_isp
o uart uses the quickdev combo cabel and runs at 115200
o compile works under os x
o seriall screen works under os x
o os x see masstorage device

